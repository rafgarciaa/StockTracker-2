import React, { FunctionComponent } from 'react';

const Tabs: FunctionComponent = () => {
  return (
    <div className="header-tabs">
      <a href="#">QUOTES</a>
      <a href="#">MARKETS</a>
      <a href="#">WATCHLISTS</a>
    </div>
  );
};

export default Tabs;
